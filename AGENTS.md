# 项目工作指引

- 开始任务先看[文档索引](docs/INDEX.md)、[当前任务](docs/work/CURRENT_TASK.md)、[实际状态](docs/work/STATUS.md)，检查工作目录、分支及全部已有修改，保留用户改动。
- 按索引读取当前任务涉及的现行正文及依赖边界，不默认通读全部docs；涉及实现时完整阅读[代码规范](docs/CODING_STANDARDS.md)。
- 只执行用户已授权的任务；待批准事项、建议和示例不自动成为产品决定或实现授权。监督／只读会话不得因本指引扩大权限。
- 按用户2026-09-14更新的分工，监督者维护项目文档、实施计划、CURRENT_TASK、STATUS及本指引，进行契约／静态审查，并仅在用户明确授权时完成指定提交；不修改业务源码、测试或工程环境，不运行项目、测试、容器或模型。执行者只负责获准的源码、测试／夹具、运行资源及必要工程配置／锁文件，不修改AGENTS.md或docs/，不撰写项目计划或报告文档；docstring和运行时prompt仍属于实施资源。
- 执行者用最终消息、自有临时目录中的原始日志及JSON清单交付结果、命令、退出码、文件指纹、限制及需修订的契约位置，由监督者核对并整理进文档。普通范围内实现选择连续处理；真实契约冲突只暂停受影响部分并报告，不自行修改正文或降低保证。
- 两个会话由用户手动转交，不使用子代理，不自动创建、唤醒或调度其他会话，不通过工具向另一会话发送任务。工作区同一时刻仅一个写入者；执行者实施期间监督者只读，文档修订在执行者停点后进行。
- `docs/product/`、`docs/modules/`、`docs/architecture/`及`docs/CODING_STANDARDS.md`分别维护现行产品、模块、架构和代码规范正文。每项规则只维护一处，其他文件用链接导航；修改时更新所属正文和受影响链接，不再同步原始文档。
- `docs/reference/`中的三份原始文档已经冻结，仅供设计和审核参考，不直接约束当前实现，也不覆盖现行正文。参考资料中的遗漏或差异作为设计建议交用户判断，不能自行恢复旧规则。迁移保留既有批准状态和关键保障，不将草案批量批准。
- 契约只记录批准状态；CURRENT_TASK负责当前目标、授权、阻塞与最近验证，保持约一页；STATUS只在验收节点简短记录里程碑、提交、验证依据及能力限制。历史通过Git定位，不追加历次全过程。
- 现行正文出现真实冲突时，注明双方位置并停止受影响部分，不以文件新旧、历史原文或代码现状自行裁决。
- 源码使用语义化名称，文件、注释、接口和测试独立可理解；实现、运行日志及运行时prompt不携带规划编号、开发阶段或设计文档引用。文档内可保留编号和交叉引用。
- 全部内部模型请求经独立Provider；日志、配置分别经统一模块。遵守现行契约的持久化确认、原子提交与恢复、入口隔离、批次冻结及三终态、梦境门控、数据所有权与权限边界。
- 检查如实记录对应版本及结果，未执行不写通过，历史检查不代表当前版本。已被替代的计划和一次性过程报告不作为现行文档保留；已提交历史通过Git定位，未提交独有信息先保全。
- 完成授权任务后按上述分工交付证据、更新必要记录并停止；安装依赖、提交、推送、合并、部署及下一项实现均按用户授权执行，不自动开展。
