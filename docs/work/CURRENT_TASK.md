# 当前任务

任务：**配置参数定义与只读注册表**。

状态：**契约已批准，待实现**。用户已批准参数定义、注册、冻结、查询、枚举、六类错误及静态校验修订，并明确使用Python 3.12与uv管理。实现测试采用标准库unittest，精确十进制使用decimal.Decimal；这些决定不需要再次审批。

后续业务代码实现尚未开始；当前工作区没有业务源码或实现测试。此前生成的源码已按用户要求删除，不计为实现成果；保留的uv配置和虚拟环境不代表切片已实现或测试通过。等待下一次代码实现授权。

## 有效契约与实施范围

批准修订已归并到[权威原文§11.9](../../companion_memory_module_design_provider_logging_config.md#configuration-registry-contract)，并同步[配置阅读视图](../architecture/configuration.md#configuration-registry-contract)。旧排除默认自洽条款及旧例子不再是有效预期。固定reason原因码统一为UPPER_SNAKE_CASE，不兼容旧小写值；字段名、配置键、触发条件与错误优先级不变。

后续获授权的实现范围仅为companion_memory/configuration、相应tests、必要根包标记和最小uv项目配置。六种声明类型、nullable、默认／枚举／数值范围静态自洽、深不可变、失败无部分修改及安全错误按已批准契约执行，不迁就代码修改预期。

## 本次文档工作

起点为main／1ce4cc2，暂存区为空；保留7份已有未提交文档改动及4个未跟踪的本地／uv配置文件。三份权威原文均已受Git跟踪，已删除指南未被跟踪，不恢复或新增替代指南。

本次仅修正文档、检查工作区并创建独立本地文档提交；按明确文件路径暂存文档基线，将.gitignore、.python-version、pyproject.toml、uv.lock及虚拟环境留在提交之外。不编写业务代码，不安装依赖，不创建迁移，不运行实现测试。

## 检查与停止

检查实际差异、受影响的相对链接、原因码一致性、任务状态及提交范围；结果以实际执行记录和交付回执为准，不预填提交成功或测试通过。提交成功后停止，不推送、合并、部署或自动开始实现。

本次文档检查：`git diff --check`无警告；本机Python临时脚本检查48份Markdown中的1956处相对链接及显式锚点，无缺失目标或重复显式锚点。当前配置契约与对应视图一致，11种结构／操作原因及10种静态原因均为UPPER_SNAKE_CASE；定义字段、配置键、六类错误触发条件及优先级未变。46份待提交文件均为文档，其中38份无关主题视图仅修正公共维护提示；已检查新增正文中的常见凭据模式及文件范围，未发现误加入密钥或本地生成文件。以上仅为文档检查，不是实现测试；本次未运行实现测试、未安装依赖。提交是否成功及准确标识以实际Git结果为准。

CURRENT_TASK.md记录当前任务；STATUS.md在切片完成节点简短更新。ORGANIZATION_REPORT.md仅保留一次性整理历史，不持续追加代码任务的哈希、全文还原或全量覆盖映射。

## 后续边界

不实现配置持久化、环境或文件加载、有效配置解析、完整权限系统、跨参数业务校验、热修改、Web、Provider、日志系统或迁移；不填全系统配置项，不创建其他模块骨架，不引入第三方依赖。将来实现并完成真实检查后才可标记“实现完成，待验收”，不得标记整个配置模块完成。
