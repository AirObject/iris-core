# 单一工作包执行队列

2026-09-07 项目负责人决定：停止“根据构建文档实现 Phase 14”整体 goal。当前没有活动 goal；本页是待办与交接，不自动启动下一项。Phase 14 尚未完成，也未进入稳定发布验收。

## 已保存成果

- 主目录实现检查点：`eda18d2`。保留 Schema 20、已验收 Policy 基线及尚未通过组合验收的并发反馈修正。最新全量 CI 为 11680 passed / 1 failed，Hybrid Recall 单次请求超出 deadline；局部诊断 2 passed，不能代替全量通过。详见[验证记录](../reports/phase-14-verification.md)。
- 未合入 Draft 原型：`codex/phase14-persona-drafts-checkpoint`，提交 `e639a2d`，基于上述主目录检查点保存 22 个差异文件。包含独立 Draft 版本、Schema 21 原型、授权命令与删除恢复账本，以及未完成的上下文/API。此前 22 个定向测试通过；最后的上下文/API 编辑尚未复验，路由未注册、请求契约/UI/Hold 审查/完整 Schema 21 回归未完成，ADR 仍为 Proposed。该分支用于续作，不能直接作为已验收版本合入。

## 下达与收尾规则

每次只下达一个工作包，写明需求 ID、交付范围、必要依赖和验收清单。完成后记录提交与实测结果，结束该目标；相邻功能、新发现问题和发布演练归入具名后续工作包，不自动扩大当前目标。

开发中运行受影响测试及必要的类型、契约、真实浏览器或迁移检查；Python 子集使用 `make test-affected TESTS="具体测试路径或 node ID"`。每工作包收尾执行一次 `make ci`，保留原有覆盖率与性能阈值。失败后定位并复测受影响项；仅在修正或明确的集成疑点需要时再次全量，不因未通过就自动循环执行无变化候选。检查点提交不等于验收通过，未关闭的失败必须明示。

## 交互式工作包队列

下列均为待下达项；编号沿用[Phase 13](./phase-13-web-console.md)与[Phase 14](./phase-14-hardening-release.md)，不重复定义领域语义。

| 工作包 | 单一交付范围 | 收尾条件 |
| --- | --- | --- |
| 13.6 统计（下一项） | P13-STATS-01；按设计 §8 完成指标注册表、八个统计面、rollup/回填、Recall 耗时采集及 Console 展示 | 权限可见集合、250ms 查询超时降级、新鲜度/lag、缺失值语义、真实浏览器和契约验证；工作包末尾全量 CI |
| 13.4 Persona Draft | 在保存分支上补齐独立草稿生命周期，审查后整合；同时承接 Persona 并发反馈的未关闭验证 | 正式 ADR/契约、真实 HTTP/UI、CAS/授权/Hold/删除恢复及 Schema 升级验证；工作包末尾全量 CI |
| 13.5 保留与 Hold | 仅剩余保留策略、Hold 及其删除链路 | 按设计权限与删除不复活要求验证；工作包末尾全量 CI |
| 13.7 导出 | 一致快照、JSONL/CSV、下载授权和过期 | 导出契约、转义/隔离、真实流程；工作包末尾全量 CI |
| 13.8 导入 | 依赖 13.7；staging、审核/备份、提交、去重/断点/补偿 | 拒绝清单、恢复与真实导入链；工作包末尾全量 CI |
| 13.9 Provider | 配置、探测、激活与运行接线 | 出站/密钥隔离、Generation 切换和回滚；工作包末尾全量 CI |
| 13.10 Settings | 注册表、多键修订及运行生效 | 每键实际行为、历史/回退、pending_restart；工作包末尾全量 CI |
| 13.11 运维 | 未完成的任务/DLQ、重建/备份、审计、探针与手册 | 实际运维流程与授权；工作包末尾全量 CI |
| 14.2 生产装配 | 镜像、部署、健康与独立 OS 身份/目录隔离 | 目标环境启停与隔离证据；工作包末尾全量 CI |
| 14.3 安全与供应链 | 凭据生命周期、恢复安全、扫描与产物证明 | 该工作包安全清单与处置证据；工作包末尾全量 CI |
| 14.6 发布验收 | 汇总同一冻结 RC 的已完成证据和 Manifest | 前置批次全部有合格结果后单独下达，不在等待期保持交互式 goal |

13.6 的领域语义以[统计设计](../design/console-backend.md#8-统计与运行观测)为准；尚未实现的 Provider 等指标应如实表达不可用或缺失，不能为实现统计自动扩展到 13.9。若前置能力缺失使既定要求无法完成，记录明确依赖，不虚构指标或完成状态。

## 14.4 / 14.5 独立后台批次

两项状态均为待排期，当前未创建定时任务或启动长跑。目标环境、冻结候选及启动窗口尚未确定；先完成对应运行脚本与前置能力，再单独下达执行。

| 批次 | 输入与前置条件 | 固定执行范围与停止条件 | 归档产物 |
| --- | --- | --- | --- |
| 14.4 性能/容量/Soak/故障 | 冻结 Commit/镜像摘要、目标硬件、数据规模与预先确定的预算 | 一轮基线/容量/故障矩阵及至少 24h 的混合 Soak；启动时固定总时限，到期结束，失败即记录，不自动无限重试或修改候选 | 数据集与参数、逐阶段日志、延迟/容量/资源曲线、故障证据、通过/失败报告 |
| 14.5 恢复/升级/回退 | 已验证备份、删除/授权账本与密钥恢复方案、隔离环境、冻结候选 | 3 轮安装→Seed→升级→备份→恢复→回退；启动时固定每轮及总时限，失败保留现场并结束批次 | 备份与候选摘要、完整性/权限/不复活检查、RPO/RTO、运行手册与逐轮结果 |

未来后台任务仅在完成、失败或需要处理时通知；运行期间不自动扩展范围，不让交互式 goal 等待 24h。性能和恢复阈值仍遵守原阶段文档，拆分执行不代表取消发布要求。
