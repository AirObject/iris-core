# 阶段 N：名称

> 状态：Planned  
> 前置阶段：对应阶段链接  
> 更新日期：YYYY-MM-DD  
> 架构依据：带章节锚点的架构/ADR 链接（创建阶段时替换）

## 阶段目标

说明新增的可验证能力，以及当前已有的可复用基础。

## 架构约束

引用相关不变量，只补本阶段特有的边界。

## 需求追踪

| 需求 ID | 工作包 | 验证证据 |
| --- | --- | --- |
| PN-EXAMPLE-01 | N.1 | 对应测试/演练及通过条件 |

## 工作包

### N.1 工作包名称

写清输入依赖、实现/契约/迁移产物及通过条件；标注当前进度，避免目标与完成事实混写。

## 数据、契约与回退策略

说明版本真源、兼容范围、迁移和失败/回退路径；无变更时说明依据。

## 量化验收基线

给出数据规模、环境、阈值、采样和失败条件，不能执行后按结果倒填目标。

## 退出门禁

- [ ] 领域行为与不变量。
- [ ] 契约、故障、并发、迁移或真实 E2E。
- [ ] 文档、版本及交付证据。

## 交付证据

实施后链接代码/变更、ADR、Schema/Migration、测试报告与已知限制；标明日期、环境和实际命令，不为未执行门禁填写通过结果。

## 明确不做

列出有依据的非目标，未完成的承诺不得放入此处静默取消。

## 交接条件

说明下一阶段可依赖的接口/行为、未关闭事项的工作包归属及阻塞条件。
