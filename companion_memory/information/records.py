"""Compatibility imports for closed records; persistence owns these pure helpers."""
from companion_memory.persistence.record_primitives import (
    ID, COUNT, REVISION, TIME, BOOL, VERSION, TEXT, Record, choice, record, integer,
    text, checked, decode, identity, TARGET, TARGETS, FACT, fact, EXPECTED, ITEMS, COMMAND_INPUT,
)
