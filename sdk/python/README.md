# Iris Memory Python SDK

The Phase 0 SDK provides dependency-free async capability negotiation, stable error models, and forward-compatible fixture validation. Domain operations are added only when their phase contracts freeze.
